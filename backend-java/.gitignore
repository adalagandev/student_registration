# Maven build output — compiled classes and the packaged jar are regenerated by
# `mvn package`, so they don't belong in version control.
target/

# IDE/project metadata that some editors drop next to the pom.
.idea/
*.iml
.vscode/
